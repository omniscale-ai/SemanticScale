# SH2 Activation Steering — Dependencies
# GPU server: CUDA 12+ required for generative model inference

# Core
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
bitsandbytes>=0.41.0        # For 4-bit/8-bit quantization if VRAM-constrained
safetensors>=0.4.0

# SciBERT evaluation embedding
# (transformers covers this, but listing explicitly)
sentencepiece>=0.1.99

# Scientific computing
numpy>=1.24.0
pandas>=2.1.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
pyyaml>=6.0
jsonlines>=4.0.0
tqdm>=4.66.0

# Optional: for baukit-style activation hooks (alternative to manual hooks)
# baukit>=0.1.0
