paths:
  data_dir: ../../data/sh6
  reports_dir: reports

model:
  name: gpt-5.4-nano
  service_tier: flex
  reasoning:
    effort: medium
    summary: auto

dataset:
  hf_path: openai/frontierscience
  split: test
  max_samples: null   # null = run on full dataset

inference:
  max_concurrent: 20
  max_retries: 5
  retry_min_wait: 1.0
  retry_max_wait: 60.0

grading:
  grader_model: gpt-5.4-nano
  service_tier: flex
